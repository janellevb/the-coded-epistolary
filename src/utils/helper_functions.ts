export function get20PercentFromTop() : number {
	const screenHeight = window.innerHeight;
	return screenHeight * 0.1;
}

export function getMiddleX() : number {
	const screenWidth = window.innerWidth;
	return screenWidth * 0.5;
}
